def divide_numbers(x, y):
    try:
        result = x / y
        print(f"{x} / {y} = {result}")
    except ZeroDivisionError:
        print("Error: Division by zero is not allowed.")
divide_numbers(10, 2)
divide_numbers(5, 0)