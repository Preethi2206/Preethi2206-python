def calculate_grade(score):
    if score >= 90:
        return 'A'
    elif score >= 80:
        return 'B'
    elif score >= 70:
        return 'C'
    elif score >= 60:
        return 'D'
    else:
        return 'F'
test_cases = [85, 60, 95]
expected_outputs = ['B', 'D', 'A']

for i, score in enumerate(test_cases):
    output = calculate_grade(score)
    expected_output = expected_outputs[i]
    print(f"Test Case {i+1}: Input score: {score}. Expected output: Grade {expected_output}. Actual output: Grade {output}.")
